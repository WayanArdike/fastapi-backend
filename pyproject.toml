[project]
name = "fastapi-example"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "mysql-connector-python==8.3.0",
    "pandas",
    "scikit-learn",
    "httpx",
    "python-multipart"
]